- hosts: all
  become: true
  become_user: root
  tasks:
    - name: Run Update nextcloud script
      command: /var/scripts/update.sh
      register: script_output
      changed_when: true

    - name: Display script output line by line
      debug:
        msg: "{{ item }}"
      loop: "{{ script_output.stdout_lines }}"
